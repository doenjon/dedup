workDir = "/scratch/users/jdoenier/dedup_benchmark/"

params {

        prefix = "Av"
        results = "$SCRATCH/dedup_benchmark_results/${prefix}/"

        long_reads = "$SCRATCH/dedup_benchmark/Av/SRR13348928.fastq"
        illumina_reads = [["$SCRATCH/dedup_benchmark/Av/SRR13348927_1.fastq", "$SCRATCH/dedup_benchmark/Av/SRR13348927_2.fastq"]]

        // specific parameters for purge_haplotigs
        l = 40
        m = 100
        h = 160
}
