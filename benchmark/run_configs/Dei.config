workDir = "/scratch/users/jdoenier/dedup_benchmark/"

params {

        prefix = "Dei"
        results = "$SCRATCH/dedup_benchmark_results/${prefix}/"

        ont_reads = "$SCRATCH/dedup_benchmark/data/SRR27077961.fastq"
        illumina_reads = []
        pacbio_reads = "$SCRATCH/dedup_benchmark/data/SRR27077960.fastq"
        
        genome = "$SCRATCH/dedup_benchmark/data/Di/GCA_039602395.1_SBBG_Deinvi_1.0_genomic.fna"

        l = 40
        m = 100
        h = 160
} 