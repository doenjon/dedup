workDir = "/scratch/users/jdoenier/dedup_benchmark/"

params {

        prefix = "Pt"
        results = "$SCRATCH/dedup_benchmark_results/${prefix}/"

        long_reads = "$SCRATCH/dedup_benchmark/Pt/ERR6668574.fastq"
        illumina_reads = [["$SCRATCH/dedup_benchmark/Pt/ERR7929069*1*.fastq", "$SCRATCH/dedup_benchmark/Pt/ERR7929069*2*.fastq"]]

        // specific parameters for purge_haplotigs
        l = 40
        m = 100
        h = 160
}
