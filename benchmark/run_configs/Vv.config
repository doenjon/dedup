workDir = "/scratch/users/jdoenier/dedup_benchmark/"

params {

        prefix = "Vv"
        results = "$SCRATCH/dedup_benchmark_results/${prefix}/"

        ont_reads = "$SCRATCH/dedup_benchmark/data/SRR3321323.fastq"
        illumina_reads = []
        pacbio_reads = []
        genome = null

        l = 40
        m = 100
        h = 160
} 